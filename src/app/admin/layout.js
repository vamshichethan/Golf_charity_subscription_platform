export const metadata = {
  title: "Admin Panel | SwingForGood",
  description: "Admin dashboard for managing the SwingForGood golf charity platform.",
};

export default function AdminLayout({ children }) {
  return (
    <>
      {children}
    </>
  );
}
